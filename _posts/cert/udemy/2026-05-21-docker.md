## Introduction
Docker packages applications and their dependencies into portable containers that run consistently across hosts. Containers are lightweight, process-isolated units that share the host kernel while keeping separate filesystem and network views.

## Quick overview

- Image: a read-only, layered filesystem (built from a Dockerfile).
- Container: a runtime instance of an image (writable top layer).
- Registry: stores and distributes images (Docker Hub, private registries).
- Docker Engine: the daemon that manages images and containers (it uses containerd and an OCI runtime).

### Architecture (visual)

Traditional stack:

```text
+------------------------+
|        Apps            |
+------------------------+
|   Operating System     |
|      (Host OS)         |
+------------------------+
| Physical Server / VM   |
+------------------------+
```

Containerized stack:

```text
+------------------------------------------------+
| Containers                                     |
| +--------+  +--------+   +--------+            |
| |App 1   |  |App 2   |   |App 3   |            |
| |bins/lib|  |bins/lib|   |bins/lib|            |
| +--------+  +--------+   +--------+            |
+------------------------------------------------+
| Docker Engine (container runtime)              |
+------------------------------------------------+
| Operating System (Host OS)                     |
+------------------------------------------------+
| Physical Server / VM                           |
+------------------------------------------------+
```

### How containers work (technical)

- Namespaces: isolate kernel resources (pid, net, mnt, ipc, uts, user).
- cgroups: limit and account for resources (CPU, memory, I/O).
- Union filesystems (overlayfs): implement layered images with a writable container layer.
- Runtime stack: Docker Engine → containerd → runc (OCI runtime) launches container processes.

Images are built as layers (Dockerfile instructions produce new layers); the union FS composes layers into a single filesystem for a container.

### Image & container lifecycle

- Build: `docker build` (image layers, cache).
- Distribution: `docker push` / `docker pull` to/from registries.
- Run: `docker run` starts a container; `docker stop` gracefully stops it.
- Inspect / Cleanup: `docker inspect`, `docker logs`, `docker rm`, `docker rmi`.

### Core components

- `Dockerfile`: build recipe for images.
- Images: layered, immutable artifacts.
- Docker Engine (daemon + API).
- `containerd` and `runc`: low-level runtime components.
- Registry: public or private image storage.

### Common commands (quick reference)

- `docker build -t name:tag .` — build image
- `docker run --rm -it name:tag` — run interactive container
- `docker ps -a` — list containers
- `docker images` — list images
- `docker pull name:tag` / `docker push name:tag` — exchange images with registry

### Security & best practices

- Run processes as non-root inside containers when possible.
- Use minimal base images and scan images for vulnerabilities.
- Drop unnecessary Linux capabilities; use seccomp and AppArmor/SELinux profiles.
- Limit host mounts and avoid privileged containers; prefer read-only root filesystems.
- Consider rootless containers or user namespaces for stronger isolation in multi-tenant hosts.

### Containers vs virtual machines (concise)

- Containers share the host kernel: faster startup, smaller footprint.
- VMs include a full guest kernel: stronger isolation, heavier resource use.
- Use containers for microservices, CI/CD, and scalable deployments; use VMs when kernel-level separation is required.

In short,Docker leverages kernel features (namespaces, cgroups, union filesystems) to provide reproducible, efficient application environments. Images, registries, and a standardized runtime stack make building, sharing, and running applications simpler and more consistent across development and production.

## Linux commands
It is strongly recommended that you already know Linux commands because the Docker engine layer uses

Linux or Linux like commands.

So if you are familiar with Linux commands, it will make this course a lot easier.

Now I'll go briefly over some of the commands you might use as a Linux user.

If you're already familiar with Linux, you can skip this video.

So Linux is a command line interface, and so you can enter all kinds of commands.

For example, if you want to know your username, you can type the command.

Who am I?

Press enter and it shows your username.

Or maybe you want to repeat some text.

You can use the echo command

and that will repeat whatever you type between those quotes.

So a command is anything you type and press enter that then gives a certain output.

Now Linux has all kinds of commands and you can do, for example, interact with the file system, you

can interact with files with the network and so on.

So some basic commands is the LS command, the list command.

It lists files in the current directory you can enter a certain directory.

For example, we can enter the pictures directory and to list the files we type ls again to go up a

directory we can type CD dot dot enter to output a file.

You can use the cat command.

Now there's no file in this system, but with the cat commands you can show a file to show our current.

To show the current working directory you can type PWD.

So those are some of the commands you can use to interact with the file system, to show the running

processes.

You can use the PS command.

So PS will show you the running processes.

Now to show all of them you can do PS minus Capital A, that will show you all of the running processes

and those are some of the Linux commands that you might use through the course when interacting with

Docker.

Docker also works on the command line, so you type commands as well.

If we type Docker version, for example, it shows you the Docker version.

So note that I typed Docker version here as commands.

So there's all kinds of commands with Docker in front.

You can type Docker PS showing the Docker containers.

Currently there's no containers.

We can run a container with Docker run and start a container, stop a container and so on.

So if you are a little bit familiar with the Linux command line, that will help you to learn about

Docker as well.

Let's go over some more basic Linux commands for the file system.

You can use the touch command to create a file.

So touch with your file name.

Let's say hello dot txt that creates the file.

Hello.

You can create a directory with the mkdir command.

If you want to delete something you can use the command.

So hello.txt.

If you want to delete a directory you can just delete it by typing.

Hello.

Because it says it's a directory so it needs to delete every file in that directory as well.

Even though in this case there are no files in that directory.

So add the minus r flag.

When you want to delete a directory, you can copy files, move files as well.

So if we create a file and let's say we can move that file so we can say move hello, text to documents.

And now that file has been moved to documents.

Now on Linux, you can also do text editing in the command line.

Different text editors for that exist.

For example, nano.

We can use Nano to create a new text file.

Now remember we moved it to documents so there is no hello txt in the current directory.

We can type some text to save press ctrl o.

So let's hold the ctrl key and press enter to quit press ctrl and x to output it.

We can type cat.

Hello dot txt that outputs the text.

So nano is just one of the possible text editors.

Only in Linux there's all kinds of text editors, so if you want to quickly edit some text, you can

do that with Nano.

If you want something more advanced, you can use Vim.

But keep in mind it has a very strong learning curve.

So you might spend a month learning Vim as opposed to Docker.

And so you could use Nano right now or just use one of the graphical editors on your system.

It files if you need to.

So you saw how to create files, directories, how to remove them.

You can also copy them so we can say copy.

Hello txt to hello two dot txt.

Then we have a copy to see the contents of a file we can say to use the cat command that outputs the

contents of a file.

So some of the basic commands, there's also the file command which shows you information about a file.

So in this case it shows that it's a text file.

So those are some of the basic Linux commands for far as files are concerned.

So that later when you use Docker, you, you are those commands won't fly will be obvious that they

are Linux commands.

And so you might use one or more of these commands later on.

It's recommended to learn more about the Linux command line for learning Docker, but it all should

be possible to follow along.


## Docker images
