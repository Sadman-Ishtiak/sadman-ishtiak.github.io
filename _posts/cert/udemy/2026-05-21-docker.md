## Introduction
Docker packages applications and their dependencies into portable containers that run consistently across hosts. Containers are lightweight, process-isolated units that share the host kernel while keeping separate filesystem and network views.

## Quick overview

- Image: a read-only, layered filesystem (built from a Dockerfile).
- Container: a runtime instance of an image (writable top layer).
- Registry: stores and distributes images (Docker Hub, private registries).
- Docker Engine: the daemon that manages images and containers (it uses containerd and an OCI runtime).

### Architecture (visual)

Traditional stack:

```text
+------------------------+
|        Apps            |
+------------------------+
|   Operating System     |
|      (Host OS)         |
+------------------------+
| Physical Server / VM   |
+------------------------+
```

Containerized stack:

```text
+------------------------------------------------+
| Containers                                     |
| +--------+  +--------+   +--------+            |
| |App 1   |  |App 2   |   |App 3   |            |
| |bins/lib|  |bins/lib|   |bins/lib|            |
| +--------+  +--------+   +--------+            |
+------------------------------------------------+
| Docker Engine (container runtime)              |
+------------------------------------------------+
| Operating System (Host OS)                     |
+------------------------------------------------+
| Physical Server / VM                           |
+------------------------------------------------+
```

### How containers work (technical)

- Namespaces: isolate kernel resources (pid, net, mnt, ipc, uts, user).
- cgroups: limit and account for resources (CPU, memory, I/O).
- Union filesystems (overlayfs): implement layered images with a writable container layer.
- Runtime stack: Docker Engine → containerd → runc (OCI runtime) launches container processes.

Images are built as layers (Dockerfile instructions produce new layers); the union FS composes layers into a single filesystem for a container.

### Image & container lifecycle

- Build: `docker build` (image layers, cache).
- Distribution: `docker push` / `docker pull` to/from registries.
- Run: `docker run` starts a container; `docker stop` gracefully stops it.
- Inspect / Cleanup: `docker inspect`, `docker logs`, `docker rm`, `docker rmi`.

### Core components

- `Dockerfile`: build recipe for images.
- Images: layered, immutable artifacts.
- Docker Engine (daemon + API).
- `containerd` and `runc`: low-level runtime components.
- Registry: public or private image storage.

### Common commands (quick reference)

- `docker build -t name:tag .` — build image
- `docker run --rm -it name:tag` — run interactive container
- `docker ps -a` — list containers
- `docker images` — list images
- `docker pull name:tag` / `docker push name:tag` — exchange images with registry

### Security & best practices

- Run processes as non-root inside containers when possible.
- Use minimal base images and scan images for vulnerabilities.
- Drop unnecessary Linux capabilities; use seccomp and AppArmor/SELinux profiles.
- Limit host mounts and avoid privileged containers; prefer read-only root filesystems.
- Consider rootless containers or user namespaces for stronger isolation in multi-tenant hosts.

### Containers vs virtual machines (concise)

- Containers share the host kernel: faster startup, smaller footprint.
- VMs include a full guest kernel: stronger isolation, heavier resource use.
- Use containers for microservices, CI/CD, and scalable deployments; use VMs when kernel-level separation is required.

In short,Docker leverages kernel features (namespaces, cgroups, union filesystems) to provide reproducible, efficient application environments. Images, registries, and a standardized runtime stack make building, sharing, and running applications simpler and more consistent across development and production.

## Linux commands
It is strongly recommended that you already know Linux commands because the Docker engine layer uses

Linux or Linux like commands.

So if you are familiar with Linux commands, it will make this course a lot easier.

Now I'll go briefly over some of the commands you might use as a Linux user.

If you're already familiar with Linux, you can skip this video.

So Linux is a command line interface, and so you can enter all kinds of commands.

For example, if you want to know your username, you can type the command.

Who am I?

Press enter and it shows your username.

Or maybe you want to repeat some text.

You can use the echo command

and that will repeat whatever you type between those quotes.

So a command is anything you type and press enter that then gives a certain output.

Now Linux has all kinds of commands and you can do, for example, interact with the file system, you

can interact with files with the network and so on.

So some basic commands is the LS command, the list command.

It lists files in the current directory you can enter a certain directory.

For example, we can enter the pictures directory and to list the files we type ls again to go up a

directory we can type CD dot dot enter to output a file.

You can use the cat command.

Now there's no file in this system, but with the cat commands you can show a file to show our current.

To show the current working directory you can type PWD.

So those are some of the commands you can use to interact with the file system, to show the running

processes.

You can use the PS command.

So PS will show you the running processes.

Now to show all of them you can do PS minus Capital A, that will show you all of the running processes

and those are some of the Linux commands that you might use through the course when interacting with

Docker.

Docker also works on the command line, so you type commands as well.

If we type Docker version, for example, it shows you the Docker version.

So note that I typed Docker version here as commands.

So there's all kinds of commands with Docker in front.

You can type Docker PS showing the Docker containers.

Currently there's no containers.

We can run a container with Docker run and start a container, stop a container and so on.

So if you are a little bit familiar with the Linux command line, that will help you to learn about

Docker as well.

Let's go over some more basic Linux commands for the file system.

You can use the touch command to create a file.

So touch with your file name.

Let's say hello dot txt that creates the file.

Hello.

You can create a directory with the mkdir command.

If you want to delete something you can use the command.

So hello.txt.

If you want to delete a directory you can just delete it by typing.

Hello.

Because it says it's a directory so it needs to delete every file in that directory as well.

Even though in this case there are no files in that directory.

So add the minus r flag.

When you want to delete a directory, you can copy files, move files as well.

So if we create a file and let's say we can move that file so we can say move hello, text to documents.

And now that file has been moved to documents.

Now on Linux, you can also do text editing in the command line.

Different text editors for that exist.

For example, nano.

We can use Nano to create a new text file.

Now remember we moved it to documents so there is no hello txt in the current directory.

We can type some text to save press ctrl o.

So let's hold the ctrl key and press enter to quit press ctrl and x to output it.

We can type cat.

Hello dot txt that outputs the text.

So nano is just one of the possible text editors.

Only in Linux there's all kinds of text editors, so if you want to quickly edit some text, you can

do that with Nano.

If you want something more advanced, you can use Vim.

But keep in mind it has a very strong learning curve.

So you might spend a month learning Vim as opposed to Docker.

And so you could use Nano right now or just use one of the graphical editors on your system.

It files if you need to.

So you saw how to create files, directories, how to remove them.

You can also copy them so we can say copy.

Hello txt to hello two dot txt.

Then we have a copy to see the contents of a file we can say to use the cat command that outputs the

contents of a file.

So some of the basic commands, there's also the file command which shows you information about a file.

So in this case it shows that it's a text file.

So those are some of the basic Linux commands for far as files are concerned.

So that later when you use Docker, you, you are those commands won't fly will be obvious that they

are Linux commands.

And so you might use one or more of these commands later on.

It's recommended to learn more about the Linux command line for learning Docker, but it all should

be possible to follow along.


## Docker images
The images are instructions for what a container should execute.

So if there is no image, a container does nothing.

So first we need an image.

We can, for example, grab the Nginx web server image.

And to download that image we use the Docker Command, Docker pull.

So type Docker pull and then the name of the image you want to download.

In this case we want Nginx.

And with this command it will download the Nginx image.

You'll see it starts to download.

Takes a moment to download the image depending on the size of the image.

So by running this command the Docker pull Nginx command, we download the latest version of the image

nginx.

The Docker Image command allows you to do more things with images.

So just by typing Docker image and pressing enter, you'll see many different commands you can use.

So for example, we can list the images on the system by typing Docker image LS and you'll see I have

two images on the system.

I have the Hello world and the Nginx image.

Now see there's with the Docker image command.

It shows you all commands that we can do with the Docker image.

So I just showed you listing commands.

We and pull commands.

We can pull an image from a repository.

We can also remove an image.

So let's try to remove the nginx image by typing Docker image, Nginx and press enter and that's deleted

the image.

So if we do docker image LS again, you should see we only have one image now the nginx image has been

removed from the system.

So the command docker image shows you all images stored on the local system.

So all these images can be downloaded from the internet.

And when you type Docker image LS, it shows the images that just exist on your computer.

Now you can download all kinds of images.

For example, we can pull Nginx, the web server we had before.

You might want to pull complete systems like ubuntu and depending on the image on the software can be

larger.

So the nginx image is about 142MB.

Whereas for example something like ubuntu is 77.

So let's pull ubuntu.

It will take a moment to pull in this case.

29th May megabytes We can type Docker image LS and now it shows you the images on the system Nginx image

being a bit larger than the Ubuntu operating system, but that's how you can basically work with images.

Now if you want to remove an image, remember you can do docker image and then either use the name or

the image id, so whichever you prefer.

I'm going to just use the name and if you press enter it will remove the image.

If you then list, you'll see that the image has been removed.

There's no ubuntu anymore in the images on the computer.

So those are the basic docker image commands.

You can find all kinds of images for Docker online on the website, hub.docker.com.

And so if you go to Hub.docker.com, you'll see this screen.

And then on top you can search, for example, we can search for Linux and then select images.

Then we can find all kinds of Linux images.

So we have Linux here, Ubuntu, Linux, Debian, Linux, Amazon, Linux, Oracle, Linux, all kind

of different Linux based images for your container.

You can find all kinds of different systems and not just fighting systems.

Of course you can search any system here, for example windows and you'll see windows related containers,

but also more old things.

So if we search for images, we can find all kinds of images.

Let's say we search for server, then we can find all kinds of servers like a reddis database server,

a web server DB, So another database server, and you can find all kinds of different images that you

might use to create your container.

So it's just a little note on a website that might be interesting, which is hub.docker.com, where

you can find or browse a lot of images and then to install it you would simply type Docker pool with

the name of the image.

So let's say we pick image here, let's say Reddis.

Now we know there's a reddis image, so we just type Docker pool Reddis and that downloads the Reddis

image to your computer.

Now this works for any of these images in Hub.docker.com, so you can also search for trusted content

or verified publisher if you want, because anybody can put in here.

So maybe you only want the official ones by selecting that you only get the official Docker images and

they have a lot of images.

So in this case we searched for server, but they have a lot of server images.

You'll see ten pages of images or more than ten pages of images that you might use in your Docker container.


## Containers
Containers are created from images using the Docker run command.

So that's Docker space run.

And then you can run a container from an image.

For example, we can do the Hello world image.

So that will create a container based on the Hello World image.

You'll see this image.

All it does is show some text output.

That's all it does.

We can see the running containers with the Docker PS command.

So if we type Docker space PS, you'll see the running containers and currently no container is running

because what this does image does is it exits immediately.

So you'll see we could immediately type.

There is no running program anymore.

That's why when we type Docker PS it doesn't output anymore.

However, we can add the minus E flag and then we can also see the historical containers.

So if we type Docker PS minus a, so that's minus a it will show you all containers that are currently

running and currently and containers that have run in the past.

So if I press enter, you'll see the Hello World image using this container and it has run about 48

seconds ago so you can actually see which containers have been run.

Every container has a unique ID and also an image and you can see when it has been created and what

happened.

So you'll see status here that it has exited.

Now not all containers act immediately.

So when we run Hello World, we can immediately use our system for whatever because this container it

creates from the Hello world image immediately exits.

Now that doesn't have to be the case.

For example, you can pull a complete operating system, let's say the Debian Linux system so we can

pull the image Docker pull image, Docker pull Debian that pulls the Debian operating system image.

Then you can view it if you want to with the command Docker image.

LS It shows you the running.

So not the running, but it shows you the images on your system and what a user can do is besides just

run it once.

So this would run it once.

It can run it interactively.

So by adding minus it interactive terminal with the shell bin bash.

Then.

Sorry Debian should be after that.

We then enter the container.

So the container is still running.

And so if we can open a new tab and do Docker, PS, you'll see Debian is currently running, but if

we do it again, it's still running, it just keeps running.

It's because we are currently exit this container and so you'll see that this is a Debian system.

I can list the files in the directory.

For example, enter the etsy directory.

Again, list the files we can.

We have Debian version here.

You'll see it's Debian 12.1.

So we have a container based on the linux image.

Now it's not the only system it can run.

You can run all kinds of Linux systems inside your container and you'll see that if you try Docker PS

again it is still running so a container can exit immediately but doesn't necessarily have to.

And also you can enter a container into the shell using this command.

So this will create a new container based on the Debian image and then enter that shell.

So that is basic course.

## Dockerfiles
So far.

We created containers based on an image using a single command.

Now, that is okay if you just want a very basic container.

But what if you want something more complex?

Then you can use a dockerfile.

A dockerfile is a text file with instructions.

And using those instructions, you are constructing the container.

So let's make our own container using a dockerfile.

First we create a directory.

Hello world.

We enter that directory.

Then we need to create a dockerfile.

You can do that with any text editor.

So if you want to use nano, you can use nano.

If you want to use vim, you can use vim.

Use whatever text editor you're comfortable with.

So Dockerfile is spelled with a capital D.

So dockerfile.

Then the first thing we need to define is the image that we want to make our container from.

So we're going to use ubuntu.

Or we can use Debian.

It doesn't really matter.

So from and then the image you want to use as your base of your container.

Let's pick Debian.

Then if you want you can run a certain command.

For example, run rmi, we can create a text file so we can say run on Linux system.

That touch command makes a text file.

So let's say hello dot txt.

And let's also define our working directory.

So work there.

And then the slash directory, the root of the system.

We're going to save the dockerfile.

And now we're going to run this.

We're going to build this dockerfile.

We type Docker build minus t the name of our project and then dot the current directory.

So that builds a container or sorry, that builds an image.

And then we can use that image to to run.

So we'll say Docker run.

Once an interactive terminal to see what's going on.

Minus it.

Hello, world.

And then the shell.

And now we can look inside the system.

So you'll see that.

Hello Dot txt file has been created.

It's an empty file.

We didn't write anything into it.

And if we look in the etsy directory you can see it's a Debian system.

So we'll see Debian version 12.1.

So using the docker file you can create your custom container and it's easily to change systems.

For example, if instead of Debian you want to use ubuntu, you just change that in your text file.

Then we run it again.

We build the container the image again.

Okay.

The image has been built and then we run it with this command.

Let's take a look.

The Hello.txt file is there again.

Now, if we go into Etsy directory, you'll still see that Debian version.

However, if we output OS release it should show that we are using ubuntu which we are.

Now it shows that we're using ubuntu instead of Debian.

And so using dockerfile you can create your own your custom image based on an existing image.

So in this case we use Ubuntu as base image, specify the working directory and run some commands and

also created some file.

So you can really build your own container based on an image using a dockerfile that is a little bit

more complex.


## webservers
The container we made in the last lecture only creates a single file, so not very useful.

What we can do is more advanced containers.

We can create any kind of container.

For example, we can create a web server.

Let's create a directory for web server, then use your favorite text editor.

I'm using Vim, but it's quite an advanced text editor, so you might want to use nano or something.

If you don't know vim, then we can use the ubuntu image.

Once you can run an update.

So that would update the system right away.

We can install a web server, which could be, for example, Apache.

We we can expose the network ports.

So in order to access web app or something over the network, it always uses a port, a communication

line.

So in this case, we'll use Port 80.

And then of course, it has to run Apache.

You can do that like this.

Okay, so now we have our file.

This is a simple docker file.

Let's build it.

Let's build our container.

Or sorry, let's build our image.

So now it's building our.

Our image that starts the web server.

Now can take a moment to complete.

And then we'll run it.

And while running it, we'll expose the port to the to the Internet or sorry, to the local network

so that we can access the Web server.

And we do that with the minus P flag.

So the we before we specified expose 80, so port 80 and we'll also use port 80 on the exploding exposing

site.

And so once this is complete, we can run the commands, we can use Docker run, we use minus D because

we want to run the container as a daemon.

The web server has to continuously run.

Then the name is web server.

Now we use the minus p flag.

So on the Docker image we specify port 80 and it should specify open port 80 web server.

So now you'll see we got a code here.

If we type Docker PS, you'll see a unique identifier.

It's a web server running on Port Apache.

You'll see it's running on port 80 and exposed on Port 80.

So if we open the browser and we open it on localhost on port 80.

So this is localhost on port 80, you'll see the output of that Docker container.

And this is a web server, the Apache server running on Ubuntu and the one that is running here.

So if we were to stop the container or remove it

so we can say Docker.

Container docker container stop.

So let's say Docker container stop with the unique identifier.

It will stop the container.

Now might take a moment.

And then once you would load the web page, it wouldn't load anymore because the container has stopped.

Let's try that.

We reload it and you'll see the container is not running, so it's not running the Apache server.

Now, of course you can restart it with this command.

So Docker Run runs a container based on observer and the image web server on this port.

If we run it, you'll see it already has a container.

So we can just run it again with that name.

Or what you could do is remove that container and then restart it.

